# 前端环境配置

# 开发服务器配置
# 注意：使用 localhost 而不是 0.0.0.0 以避免 allowedHosts 错误
HOST=localhost
PORT=3000

# 后端 API 地址
REACT_APP_API_URL=http://localhost:8000

# 代理后端请求（开发环境）
PROXY=http://localhost:8000

# Webpack Dev Server Socket 配置
WDS_SOCKET_HOST=localhost
WDS_SOCKET_PORT=0
