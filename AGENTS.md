# 协作原则

本项目持续演进。这里不固化当前目录结构、技术方案或功能清单，只约定接手和修改时的
基本工作方式。

## 开始工作前

- 先理解用户目标、现有行为和相关上下文，再决定改动范围。
- 将代码、测试和 `docs/` 视为需要互相核对的证据；任何一方都可能滞后。
- 优先阅读 [`docs/开发/开发接手与变更规范.md`](docs/开发/开发接手与变更规范.md)，
  再按任务选择相关文档和代码。该文档是当前状态的导航，不是限制未来设计的永久模板。
- 进入代码前，先阅读目标功能说明书和[功能实现与变更索引](docs/开发/功能实现与变更索引.md)：说明书确认用户行为，
  开发索引确认实现链路、容易遗漏的特殊情况和修改影响面；如果开发索引缺少这些内容，先按开发规范补齐判断，再开始修改。
- 不要只按用户最新一句话定位改动。先回看同一功能已经实现的行为、关联页面、测试和移动端约束，
  将本次新增要求与现有行为逐项对照，明确哪些是新增、哪些是修正、哪些特殊情况仍需一并处理。

## 做出改变时

- 从完整用户流程考虑影响，不以“改动文件最少”为目标。
- 保持现有功能、安全边界和用户数据，除非需求明确要求改变它们。
- 允许重构、替换旧模式和引入更合适的结构；当现有规范妨碍正确实现时，应同时更新
  规范并说明理由，而不是机械遵循。
- 根据风险选择验证强度。业务规则、认证、数据存储、远端写操作和发行流程需要比纯
  文案或局部样式更完整的验证。
- 真实账号、凭据、会话、个人响应和运行数据不得进入版本控制或交付产物。
- 按变更类型检查开发规范中的影响矩阵；页面、统计、认证、缓存和远端写操作不能只检查当前文件。
- 修改完成后，用户可见变化更新功能说明书，实现入口、联动影响或验证方式变化更新开发索引；仅当确认相应内容均未变化时，
  才可以不改文档，并在交接说明中写明核查结论。

## 完成交接时

- 说明实现后的用户行为、关键设计取舍和受影响的关联能力。
- 说明实际完成的验证，以及尚未验证的部分和原因。
- 如果实现改变了架构、约束或推荐做法，同步更新文档，让下一位接手者看到当前事实。
- 交接说明要按功能行为组织，而不是只列文件：说明沿用了哪些现有行为、改变了哪些行为、检查了哪些
  联动层面，以及功能文档和测试入口是否同步。

规范服务于项目质量，而不是阻止项目进化。遇到新需求、新年级规则或教务接口变化时，
应以可验证的用户需求和更清晰的整体设计推动规范继续演进。
