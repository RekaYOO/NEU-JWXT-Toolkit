# 功能实现与变更索引

本文面向开发接手和修改，不是用户说明书。用户操作、页面文案和功能边界以 `docs/功能/`
下的对应说明为准；本文只记录接手时需要定位的实现入口、容易遗漏的特殊情况、联动影响和验证入口。

## GPA 模拟与成绩统计

### 当前实现链路

- 页面与交互：`frontend/src/components/GPACalculator.js`、同目录 CSS。
- 成绩页统计：`frontend/src/pages/ScoresPage.js`、同目录 CSS。
- 绩点策略：共享 GPA policy hook/计算模块及成绩缓存。
- 方案文件：`backend/app/routers/gpa.py`，按账号隔离保存。
- 关联功能：培养计划导入、成绩追踪快照与邮件。

### 当前行为与容易遗漏的特殊情况

- 手动课程名支持直接编辑，失焦或回车保存；只改名称不能重置学分、绩点或元数据。
- 学分必须有效且大于 0，绩点可以为 0；两者都有效后才移出“待输入”。
- 隐藏只存在当前模拟会话，不写入方案文件；隐藏课程不参与当前统计。
- 筛选或隐藏导致统计范围不完整时显示波浪线和说明；完整课程集不显示。
- 成绩明细和 GPA 模拟统一显示四位小数并复用同一绩点策略，包含 2025 级及以后规则。

### 修改时继续检查

成绩页、模拟桌面表格、移动课程卡、导入导出、撤销历史、统计卡、分页、快速筛选、待输入、隐藏、
绩点策略、成绩缓存和成绩追踪通知。

### 更新与验证

用户行为变化更新[成绩查询](../功能/成绩查询.md)或[GPA 模拟器](../功能/GPA模拟器.md)；跨功能规则还要看[成绩追踪](../功能/成绩追踪.md)。
重点测试：`frontend/src/components/GPACalculator.test.js`、成绩页测试、GPA 策略测试。

## 查询课表与教室空闲筛选

### 当前实现链路

- 页面与布局：`frontend/src/pages/TimetablePage.js`、同名 CSS。
- 请求封装：`frontend/src/services/api.js`。
- 后端路由：`backend/app/routers/timetable.py`。
- 教务适配与解析：`backend/core/timetable/`。
- 时间与冲突：`backend/core/scheduling/`。
- 个人首屏：服务器缓存、浏览器快照和课表恢复流程。

### 当前行为与容易遗漏的特殊情况

- 从其他页面重新进入个人周课表时默认当前学期本周；显式学期/周次链接和学期视图不受影响。
  ResourceStore 内存、浏览器快照、服务器启动缓存和学期接口先后到达时，不能重新应用上次保存的周次/学期，
  也不能覆盖用户进入页面后的手动选择。
- 手机当前周的默认星期使用本机日期，即使当天无课；日期变更、窗口重新聚焦或后台恢复时同步更新
  星期选择、今天标记和课情摘要。异步确认当前学期后须重新校正；手动换日及浏览其他周次不应被抢回。
- 父页日期计时器只在本机日期变化时更新，课情摘要、网格和手机课程卡的时间计时器仅在显示分钟变化时
  更新；两者均不触发课表请求或清空已挂载结果。手动切周（含桌面滚轮和手机滑动）关闭当前页面的
  手机选日自动跟随；跨日后只在尚未手动浏览且原本跟随本周时自动换周/换日，重新进入页面才重置此状态。
- 桌面周选择框用原生非被动滚轮监听避免页面同步滚动，按升序周次有边界切换；仅个人课表手机悬浮窗
  的课程信息可点击，按周日到周六及课程结束时间查找下一次课，普通周视图切周和星期、学期视图只切星期、
  缩略周视图只切周、缩略学期视图只收起。其他查询模式的课程信息为静态内容，不显示定位箭头。
  摘要自身的时钟应随时间和页面恢复更新，不能只依赖日期变更。
- 周内顺序为周日到周六；范围判断使用包含式重叠。
- 教学周选项可能包含“（本周）”和日期范围等长文本；中等宽度下操作按钮必须独立成行，选择器文本只能省略，不能覆盖冲突检测或刷新按钮。
- 特殊空闲筛选先限定普通筛选候选，再逐教室扫描并本地核验；继续查找不能重复扫描。
  默认周次只在初始条件尚未填写时设定，跨日或当前周变化不能清除已填写条件和扫描结果。
- 扫描结果与请求条件、游标、已检查教室标识同存于当前页面会话；选中候选教室时暂停扫描，重开筛选
  恢复候选且不运行普通目录预览。续查沿用首次扫描的搜索词、校区及条件，不取课表查看过程中改变的
  页面状态；主动修改筛选条件或切换学期/模式时才废弃旧扫描。
- 无法解析时间的课程不能误判为空闲；结果增量显示，暂停和扫描完成要区分。
- 个人课表优先显示旧快照，刷新失败不能伪装成无课；认证错误不能包装成普通课表错误。

### 修改时继续检查

四种课表模式、普通/特殊筛选、周次和节次选择器、共享时间模型、冲突检测、课表预览、缓存、认证错误、
桌面布局、320/375/430/768px 移动布局及选课工作台调用。

### 更新与验证

用户行为变化更新[查询课表](../功能/查询课表.md)；缓存和认证联动还要看[缓存与数据一致性](缓存与数据一致性.md)、
[WebVPN 校外访问](../功能/WebVPN校外访问.md)和[移动端能力矩阵](移动端能力矩阵.md)。
重点测试：`backend/tests/test_timetable.py`、`frontend/src/pages/TimetablePage.test.js`、
`frontend/src/pages/TimetablePage.query.test.js`（固定本机日期跨日、缓存学期确认、重新进入本周、桌面滚轮、
手机摘要落点、手动换日、跨日预览稳定性与无重复请求）及共享调度测试。

## JWXK 选课系统

### 当前实现链路

- 工作台：`frontend/src/pages/CourseSelectionWorkspacePage.js`；归档页：`frontend/src/pages/CourseSelectionArchivePage.js`。
- 前端模式与课表辅助：`frontend/src/utils/jwxkModes.js`、`frontend/src/utils/jwxkSchedule.js`。
- 后端路由/契约：`backend/app/routers/course_selection.py`、`backend/app/schemas/course_selection.py`。
- 领域逻辑：`backend/core/course_selection/`，自动任务、归档和通知共享同一服务客户端。

### 当前行为与容易遗漏的特殊情况

- 抢选 `02` 与权重 `04` 的人数口径、结果来源和操作语义分开维护。
- “已选人数 0 / 容量 0”按非本轮或不可操作占位记录处理；这是结果判定中的特殊优先级。
- 非零记录即使批次不同，只要官方提供当前可退材料，仍按可操作课程处理。
- 只读请求可恢复并最多重试一次；已发出的写请求不自动重放。不确定的抢选结果继续只读核验。
- 目录、已选结果、自动任务、归档和通知使用同一轮次归属结论。
- 轮次名称包含“人文类选修课程选课”时，在线课筛选由
  `CourseSelectionWorkspacePage.js` 在当前目录结果上本地执行：课程名称含“在线式”为在线课，
  其余为线下课；不能把它作为通用远端筛选参数，也不能在其他轮次显示。
- 抢选异常通知由 `CourseSelectionAutomationService._notify_grab_issue` 按任务、异常类型、候选
  和稳定化原因指纹去重；轮询时间、超时毫秒数等易变文本不能进入新的通知事件。入队前保留 pending
  状态，发送失败才释放，原因或候选阶段变化才允许再次通知。

### 修改时继续检查

目录和详情、已选结果、退选入口、自动任务、归档、课表预览、通知、JWXK 线路、离线边界和模式测试夹具。

### 更新与验证

用户行为变化更新[选课系统](../功能/选课系统.md)；认证和课表联动分别查看[WebVPN 校外访问](../功能/WebVPN校外访问.md)与[查询课表](../功能/查询课表.md)。
重点测试：`backend/tests/test_jwxk_foundation.py`、`backend/tests/test_course_selection_api.py`、
`backend/tests/test_course_selection_modes.py`、`backend/tests/test_jwxk_automation.py`、
`backend/tests/test_course_weight_optimizer.py`、`frontend/src/utils/jwxkModes.test.js`、
`frontend/src/utils/jwxkSchedule.test.js`、`frontend/src/pages/CourseSelectionWorkspacePage.test.js`（含人文轮次
在线课筛选），以及 `backend/tests/test_jwxk_automation.py` 中的异常通知去重用例。

## WebVPN 与认证恢复

### 当前实现链路

- 认证客户端：`backend/core/auth/client.py`。
- 会话边界：`backend/core/auth/session_manager.py`。
- 进程保活：`backend/core/auth/keepalive.py`。
- 后端认证路由：`backend/app/routers/auth.py`。
- 前端恢复与请求重试：`frontend/src/services/api.js`、`frontend/src/App.js`。
- 调用方：成绩、课表、选课、成绩追踪、创院服务和后台任务。

### 当前行为与容易遗漏的特殊情况

- 直连和 WebVPN 共享同一业务会话；可验证线路故障才自动切换，密码错误和普通业务错误不能无限换线。
- Cookie 失效、短时网络失败和暂时不可达优先走 single-flight、超时和退避；验证码、短信、二维码或凭据变更才需要人工操作。
- 只读请求恢复后最多重试一次；写请求不自动重放。恢复失败不能清空页面上下文或缓存，手动退出后不自动恢复。
- JWXK、CXCY 等服务定向恢复保持服务边界；离线模式只读，不触发恢复或写操作。

### 修改时继续检查

登录页、全局恢复弹窗、所有业务页面、后台任务、邮件恢复、二维码/短信流程、直连/WebVPN、离线模式和服务器访问网关。

### 更新与验证

用户可见变化更新[WebVPN 校外访问](../功能/WebVPN校外访问.md)及受影响功能文档；缓存联动查看[缓存与数据一致性](缓存与数据一致性.md)。
重点测试：`backend/tests/test_auth_keepalive.py`、认证客户端/Session/WebVPN 测试、`frontend/src/services/api.test.js`、
`frontend/src/App.test.js`及相关服务页面测试。

## 成绩追踪与系统邮件

### 当前实现链路

- 页面配置和状态：前端成绩追踪页面及 API 服务。
- 快照、比较和调度：`backend/core/grade_tracking/`。
- 邮件发送：系统邮件 outbox、模板和 SMTP 服务。
- 数据与认证：共享成绩缓存、绩点策略、统一认证恢复。

### 当前行为与容易遗漏的特殊情况

- 初始化和变更邮件同时提供 HTML 与纯文本，内容必须表达同一课程变化、总 GPA、策略和缓存提示。
- 去重、队列、SMTP、远程恢复和移动通知边界不因模板改动而改变。
- 追踪只读成绩和缓存；认证失败优先静默恢复，只有必须交互时才要求用户操作。
- 关闭追踪、退出登录或切换账号不能误删其他业务来源的通知或恢复状态。

### 修改时继续检查

成绩查询、GPA 模拟、培养计划、系统邮件 outbox、调度、HTML/纯文本降级、移动通知、离线边界和账号切换。

### 更新与验证

用户行为变化更新[成绩追踪](../功能/成绩追踪.md)；模板和认证联动查看[系统邮件与远程登录恢复](../功能/系统邮件与远程登录恢复.md)、
[成绩查询](../功能/成绩查询.md)和[GPA 模拟器](../功能/GPA模拟器.md)。重点测试：`backend/tests/test_grade_tracking.py`、邮件模板测试、
认证恢复测试及对应前端页面测试。
